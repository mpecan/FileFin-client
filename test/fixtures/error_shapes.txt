# 401 unauthenticated GET /api/me
unauthorized

HTTP 401

# 404 unknown media id
not found

HTTP 404

# 307 raw bytes refused for a transcode-only file (SPEC 3.4)
HTTP/1.1 307 Temporary Redirect
Location: /api/media/919ac9caad25/file/0/hls/index.m3u8

# 415 HLS refused for a direct-playable file (SPEC 3.4, the symmetric half)
not transcodable

HTTP 415

# 206 byte range on the direct-play file
HTTP/1.1 206 Partial Content
Accept-Ranges: bytes
Content-Length: 50
Content-Range: bytes 0-49/42953
Content-Type: video/mp4

# 400 bad file index: POST /api/media/{id}/progress with file: 99
bad file index

HTTP 400

# 400 rating out of range: POST /api/media/{id}/rating with {"rating": 99}
rating out of range

HTTP 400

# 200 the sidecar subtitle route, converted SRT -> WebVTT per request
HTTP/1.1 200 OK
Content-Type: text/vtt; charset=utf-8

WEBVTT

00:00:00.000 --> 00:00:02.000
Hello fixture


# login rate limiting: 8 bad passwords in a row
401 401 401 401 401 429 429 429 

# 415 the FILE route on a server with transcoding disabled - F12's own.
# transcodeEnabled: false. The hls route's 415 above is the OTHER one,
# and it is the one this client can never see (SPEC 3.4).
transcoding disabled

HTTP 415

# ...and the detail STILL reports transcode:true on that same server,
# which is what keeps the client-side guard firing.
  file 0: "transcode":true

# ...while the direct-play film is unaffected
HTTP/1.1 200 OK
Accept-Ranges: bytes
Content-Type: video/mp4
