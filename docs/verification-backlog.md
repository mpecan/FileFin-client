# Verification backlog

Claims this repository makes that **no test in it can check**, each with the
experiment that would settle it. Created at M3, when the first code that draws
something landed and the gap between "the suite is green" and "it works on a
phone" became real.

**The rule, and it is the whole point of the file: an entry with no concrete
procedure is not an entry.** Same discipline as `tool/dep-allowlist.txt`, and
for the same reason — "verify on device" is a promise, not a plan, and a
promise is what this file exists instead of.

**"Cannot be checked headlessly" is a MEASURED conclusion here, never an
assumption.** Where the measurement exists it is cited. Where a claim turned out
to be checkable after all, it is not in this file: the nested-category contract,
the poster route's 200 branch, the manifest merge and the plist parse were all
assumed device-only in the M3 plan and all turned out to be measurable, which is
why they are tests and builds rather than rows.

Rows are retired by doing them, and a retired row keeps its result rather than
disappearing.

| # | Claim | Why it cannot be checked headlessly (measured) | The experiment | What breaks if the claim is wrong | Earliest milestone |
|---|---|---|---|---|---|
| 1 | **NF2 — a 5000-item poster grid scrolls at 60fps** | `flutter test` runs `flutter_tester` under `AutomatedTestWidgetsFlutterBinding`: fake clock, frames pumped on demand, no vsync, no rasterizer, fabricated `FrameTiming`. `flutter_tools` demands a connected device for anything under `integration_test/` (`_shouldRunAsIntegrationTests`). Both measured at M3.0/M3.7 | Build `apps/mobile` in **profile** mode. Put `grid_test.dart`'s 5000-item scenario behind `IntegrationTestWidgetsFlutterBinding`, wrap a fling in `traceAction`, and read `TimelineSummary.summaryJson`: assert `missed_frame_build_budget_count == 0` and `90th_percentile_frame_build_time_millis < 16`. Run on a physical Android device at API 26 (the C5 floor, and the slowest thing supported) and on the oldest supported iPhone. Record both summaries | M3's exit criterion is met by proxy, and the proxies are bounded-widget-count, bounded-poster-requests and single-adaptation. If real frame timing is bad anyway, the cause is rasterization or decode — neither of which any proxy here touches | M4 (a device is needed for playback regardless) |
| 2 | **NF1 — cold start to a usable library view under 2s on a warm cache** | Wall-clock cold start includes process spawn, engine init, plugin registration and the first real HTTP round trip. `flutter_tester` has no process spawn and a fake clock | `flutter run --profile --trace-startup`, read `start_up_info.json`'s `timeToFirstFrameRasterizedMicros`, then add an instrumented timestamp at the first frame that shows a category row. Repeat 10 times after a reboot with `settings.json` already holding a signed-in server. Report the median and the worst | NF1 is unmeasured today. If it fails, the likely cause is the sequential `getApplicationSupportDirectory()` → settings read → login → categories chain in `main()` and `HomeRoute`, which nothing currently parallelises | M4 |
| 3 | **Android actually connects over cleartext `http://`** | `cleartextTrafficPermitted` is enforced by the OS network stack, not by anything in the APK that a test can read. The merged manifest carries the key (verified at M3.1 from `build/app/intermediates/merged_manifest/…`), which is a different claim | Install the debug APK on an API 26 device **and** an API 34 device (the default flipped at API 28). Add a server at `http://<LAN ip>:8099` and sign in. Then delete `android:networkSecurityConfig` from the manifest, rebuild, and confirm the same request now fails — a positive result alone does not show the config is what permitted it | Every plain-http server fails on device for a reason that looks nothing like its cause: the request fails at the socket, so F1 reports "nothing answered at that address" for a server that is running | M4 |
| 4 | **iOS reaches a `192.168.x.x` server, and the local-network prompt is survivable** | ATS and the iOS 14+ local-network permission are OS-enforced and involve a user-facing dialog. Nothing in `flutter_test` can present or answer one | On a physical iPhone (iOS 15 and current), add `http://192.168.x.x:8099`. Confirm the local-network prompt appears, that the usage string from `Info.plist` is what it shows, and that the FIRST request after granting succeeds. Then deny it and confirm the app reports a reachability failure rather than terminating. Repeat after a fresh install, since the prompt is once per install | Without `NSLocalNetworkUsageDescription` iOS **kills the process** at the first local-network connection. The key is present and the built binary carries it (verified at M3.1 with `plutil` against `build/ios/iphoneos/Runner.app/Info.plist`); that the prompt then behaves is not verified | M4 |
| 5 | **iOS refuses a plain-`http://` server on a PUBLIC address, and F1's warning is therefore accurate** | Same reason as row 4, and this one is a claim the app makes to the user in words | Point the app at a plain-http FileFin on a routable public address from an iPhone. Confirm the request is refused by ATS. If it succeeds, `NSAllowsLocalNetworking`'s scope is wider than documented and `add_server_page.dart`'s warning is overstated — which is worse than understated, because it tells people something is impossible when it is not | F1's warning says "plain HTTP is only permitted for servers on your local network". If that is wrong the app is lying in a dialog. `NSAllowsArbitraryLoads` is deliberately NOT set, which is what makes this the expected behaviour | M4 |
| 6 | **The artefact RUNS on the C5 floor: Android 8 (API 26) and iOS 15** | Building for a floor and running on it are different claims. `flutter build apk --debug` and `flutter build ios --no-codesign` both succeed and the merged manifest says `minSdkVersion="26"`, the built plist says `MinimumOSVersion 15.0` (both verified at M3.1) | Install and launch on a physical API 26 device and an iOS 15 device (or the oldest simulator image Xcode still ships). Browse to a detail page. Watch for a `NoSuchMethodError` from a plugin whose own floor is higher than ours | C5 is a decision (D7). If the floor is wrong, it is wrong in the pubspec of a dependency rather than in ours, and the failure is a crash on launch | M4 |
| 7 | **`ImageCache` bounds memory over a 5000-item category with real bytes** | `flutter_tester` decodes nothing at the size a device would; the tests here use an 8-byte fixture or the 593-byte captured poster, and the cache's 100 MB limit is never approached | On a device, browse a category of 5000 items **that have posters** — the seeded library has one item, so this needs a generated library (5000 folders each with a `poster.jpg`). Fling from top to bottom and back. Watch `flutter run --profile`'s memory timeline and `PaintingBinding.instance.imageCache.currentSizeBytes`; assert it stays under `maximumSizeBytes` and that RSS returns to roughly its starting value after the fling | SPEC §7's `cache/posters/` disk cache is deliberately absent at M3, on the grounds that Flutter's in-memory cache bounds this. If it does not, the app is OOM-killed on a large library — the exact case NF2 names | M4 |
| 8 | **Scroll physics and overscroll feel right under a real touch** | `flutter_tester` has no touch input; `jumpTo` and `drag` bypass the physics simulation entirely | On a device, fling the grid hard and let it settle; fling to the top and past it. Confirm the platform-appropriate overscroll (glow on Android, rubber-band on iOS) and that a fling does not stall while posters decode | The grid's tests assert widget counts at scroll offsets, which is a statement about layout and not about feel. A fling that stutters passes every test here | M4 |
| 9 | **The app looks right** | **No golden tests exist**, and that is a decision rather than an omission — see SPEC §9 and the note below | Choose ONE canonical platform (`ubuntu-latest`, matching CI), generate goldens there in a container pinned to the Flutter revision, and run `matchesGoldenFile` only on that platform. Until then: screenshots of the tree, the grid and the detail page at 320dp, 390dp and a tablet width, reviewed by a person | Nothing verifies pixels. A layout that overlaps, clips or renders white-on-white passes the whole suite. The M3 mitigation is layout assertions (`getSize`, `getTopLeft`, exact indentation arithmetic) plus `androidTapTargetGuideline`, `iOSTapTargetGuideline` and `textContrastGuideline`, which are platform-stable — and one overflow bug was in fact caught that way | M4, or whenever a canonical platform is chosen |
| 10 | **NF6 — playback survives backgrounding** — *superseded by row 19, which has the real procedure now that M4 exists* | There is no playback until M4, and `AppLifecycleState` in a test is a synthetic enum value rather than the OS actually suspending the process | Once M4 lands: play, background the app for 60s, foreground it, assert the position is where it was and audio resumed. Then background it long enough for the OS to evict the process and confirm the resume pointer (server-side, `meta.json`) still has the position | M3's job is only not to preclude it. What could preclude it is a controller cancelling work on `dispose` in a way that outlives a backgrounding — `AsyncController` cancels on dispose, and a route that is merely backgrounded is not disposed | M4 |
| 11 | **F15's OS-trusted-certificate-change arm** | Inherited from M2 (`docs/risks.md` R5). It needs a CA-signed certificate for `127.0.0.1`, which no public CA will issue | Provision a certificate for a real hostname from a real CA, point a reverse proxy in front of `filefin` at it, pin it, then replace it with a different CA-signed certificate and confirm the change blocks. `decidePin` is pure and table-tested over every combination, which confines the gap to wiring | A server pinned while self-signed that later gets a real certificate would change fingerprint. The pure decision is tested; the wiring for that specific path is not | M7 |
| 12 | **A password survives an app restart (F2's persistence half)** | `InMemorySecretStore` is the production cache and persists nothing. There is no platform store until M7, so there is nothing to test | Once M7 lands: sign in, force-quit, relaunch, and confirm the library loads without a password prompt. Then restart the SERVER (which invalidates the session, L1) and confirm F3 re-authenticates silently from the stored password | Stated debt rather than a bug: the password is re-typed on every cold start at M3. If M7's store is wrong, the symptom is identical to today's behaviour, which is why it needs its own check | M7 |
| 13 | **Text scaling, small screens and notches** | The tests set a fixed surface size (390x844, and 800x4000 for the detail content) and the default text scale. A `MediaQuery` override is a widget test, not a device | On a device, set the system font to its largest setting and walk tree → grid → detail. Repeat on a 320dp-wide phone and on a device with a display cutout. Confirm nothing clips and that `SafeArea` insets are respected | One `RenderFlex overflowed` was already caught in `ErrorPanel` at 800x600 and fixed by making it scroll. That was luck of the surface size, not coverage: nothing systematically checks the small end | M4 |
| 14 | **The live suite's widget layer actually issues its calls** | `apps/mobile/test_live/` makes its real calls in `setUpAll` and drives the widgets over those objects, because a request initiated inside a `testWidgets` body registers its timers in that body's `FakeAsync` zone and never completes (measured at M3.0, re-measured at M3.7) | On a device or emulator, run the same flow under `integration_test/` with `IntegrationTestWidgetsFlutterBinding`, where the binding is real and a widget-issued request completes normally. Assert the same things `browse_live_test.dart` asserts | The screens are proven to render what the real server sent; the wiring from `initState` through `AsyncController` to a real socket is proven only against a fake. A mistake there — a token not forwarded, a call made twice — would pass everything here. **Narrowed at M3.R:** until then the fake answered the same payload whatever argument it was handed, so the wiring was not proven against the fake either — a grid requesting category 999 kept the whole `just it` run green. `FakeLibraryApi.calls` now records the arguments and the live widget tests assert them, which closes the identifier half and leaves only the socket half open | M4 |
| 15 | **`getApplicationSupportDirectory()` returns a real, writable directory, and `settings.json` actually survives an app restart** | `main_test.dart` replaces `PathProviderPlatform.instance` with a temp directory, which proves the wiring and nothing about the plugin: the real call is a platform channel into `NSFileManager`/`Context.getFilesDir`, and `flutter_tester` has neither. Row 2 touches this call only as startup latency and row 12 is the *secret* store at M7 — nothing covers the settings file on a device | Install the release APK. Add a server, sign in, force-quit the app, relaunch: the launch screen must read **"Signed out"**, not "No server yet". Then `adb shell run-as <pkg> cat files/settings.json` and read it — the `servers[]` entry must be there and its `baseUrl` must carry no `userInfo`. Repeat on iOS, reading the app container's `Library/Application Support/settings.json` through Xcode's device window. Negative arm: `chmod 000` that directory and confirm the app **says something** rather than losing the server silently | **The failure mode is silent by construction.** `SettingsStore.read` turns every read failure into empty settings (§13, deliberate), so a directory that is wrong or unwritable looks exactly like a first launch: the user adds a server, watches it work, relaunches and finds it gone with nothing to check. M3.R made the WRITE half speak — both screens now catch `FileSystemException` and name the OS message — but only a device says whether the directory the plugin returns is the right one | M4 (a device is needed for playback regardless) |
| 16 | **A direct-play stream and a 307-redirected HLS stream both render PIXELS** | `NativePlayer.test = true` forces `vo=null`, so nothing rasterises. **The claim that `VideoController` "does not construct" was wrong and is retracted** (M4.R/G1): it constructs, `Video` builds, and what never returns is `Player.dispose()` once a controller has attached — a probe that *pumped* a `Video` is the thing M4.0 actually killed at five minutes. So those two lines are no longer uncovered and `MAX_UNCOVERED` is back at 0; what still cannot be checked headlessly is what they DRAW, which is this row | On a physical API 26 device and an iPhone: play the seeded H.264 MP4 (direct) and the seeded HEVC MKV (307 to HLS). Confirm video AND audio for both, then run `flutter run --profile` and read the timeline for a dropped-frame cliff during a seek. Record both | The headless suite proves demux, decode-to-null, position, tracks, seek and completion — nothing about the texture pipeline, which is where `media_kit_video`'s entire risk lives. If it is wrong the app plays sound over a black rectangle | M4 |
| 17 | **NF3 — seek responds within 500 ms on direct play** | `flutter_tester` has a fake clock and no rasterizer, and the headless suite's seek lands against a local file rather than over a network | On a device over LAN, seek to 10 positions spread across the seeded MP4. Stopwatch from `seek()` returning to the first `position` event past the target; report the median and the worst. Repeat on cellular | NF3 is unmeasured. If it fails on direct play the cause is byte-range latency or mpv's demuxer cache, neither of which anything here touches | M4 |
| 18 | **The session cookie reaches libmpv on a real device, not just a host libmpv** | Every M4 measurement — E2, E3, E6 and the `just test` suite — used **Homebrew's** libmpv 0.41.0. The shipped `media_kit_libs_android_video` and `media_kit_libs_ios_video` are different builds, with different TLS and HTTP backends compiled in | On a device, set mpv's log level to `debug` and read `player.stream.log`: confirm the request carries `Cookie: filefin_session=…` and gets a 200. Then delete the header from the `PlaybackRequest` in a scratch build and confirm a 401 — a positive alone does not show the header is what authorised it | SPEC §5.3's whole streaming design is `Media(httpHeaders:)`. R1 retired the redirect half against FFmpeg's libavformat; this is the header half on the binaries we actually ship | M4 |
| 19 | **NF6 — playback survives backgrounding, and an OS kill leaves the pointer right** | `AppLifecycleState` in a test is a synthetic enum the binding is handed; the OS never actually suspends or evicts the process | Play to 0:20, background for 60 s, foreground: assert the position and that audio resumes. Then background and force-kill from the task switcher, re-open the item, and confirm the resume prompt offers ~0:20. Read `meta.json` on the server to confirm the pointer | The lifecycle-`paused` report is the ONLY thing that runs before an OS kill. If it does not fire on the real platform, everything watched since the last 30-second checkpoint is lost. Supersedes row 10 | M4 |
| 20 | **libmpv's TLS behaviour on the SHIPPED Android and iOS builds** | `tls-verify` was measured against Homebrew's mpv (M4.0/E6, both directions). Which TLS backend each prebuilt was compiled against is unknown, and a build with no CA bundle would fail `tls-verify=yes` for an ordinary public certificate | Point the app at an HTTPS reverse proxy with a self-signed certificate. Confirm D10's refusal with the override off. Then turn the override on and confirm it plays. Then, in a scratch build, force `tls-verify=yes` on that same server and confirm it FAILS — proving the property is load-bearing on device, not merely accepted | D10 rests on `tls-verify` doing something. If the shipped libmpv ignores it, an `osTrustedTls` server is silently unverified — the case D10 believes is safe | M4 |
| 21 | **The metered guard fires on real cellular** | `connectivity_plus` is a method channel; `ConnectivityPlatform.instance` substitutes headlessly (measured, M4.0/E7) and that proves the mapping, not what the OS reports | Wi-Fi off, cellular on: open a file above the threshold and confirm the prompt names the real size; enable Wi-Fi-only and confirm the refusal. **Then repeat over a tethered phone hotspot and record what `connectivity_plus` actually returns** | C4's residual: the plugin reports a transport, not a cost, so a metered hotspot is expected to arrive as `wifi` and the guard never to fire — which is the exact case F13 exists for. `decision.dart` says so; only a device says whether it is true | M4 |
| 22 | **An MKV direct-plays in a GATED suite, not only in a spike** | `tool/spikes/e5_mkv_direct_play.sh` measures both arms live — an unprobed VP9/Opus `.mkv` 307s, the same row probed answers `200` with `Accept-Ranges` — but the spike is not part of `just it`, because `tool/testserver/seed.sh` never probes and adding a third seeded item rewrites six name-and-count assertions plus the captured category fixtures | Add a VP9/Opus `.mkv` under its own category in `seed.sh`, follow the rebuild with `POST /api/admin/probe/scan` and a wait on `media_files.container <> ''`, re-capture the category fixtures, then assert `200` + `Accept-Ranges` on `.../file/0` and play it through `MediaKitPlaybackHost` in `test_live/` | M4's exit criterion is met by measurement rather than by a gate, and a measurement in a script nobody runs is one server upgrade away from being wrong without anyone noticing. It is also the only way `fileNeedsTranscode`'s PROBED branch is exercised at all — every seeded row takes the extension fallback | M5 |
