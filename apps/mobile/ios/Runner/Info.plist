<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CADisableMinimumFrameDurationOnPhone</key>
	<true/>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>FileFin</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>FileFin</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(FLUTTER_BUILD_NAME)</string>
	<key>CFBundleSignature</key>
	<string>????</string>
	<key>CFBundleVersion</key>
	<string>$(FLUTTER_BUILD_NUMBER)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>NSAppTransportSecurity</key>
	<dict>
		<!-- F15 permits a plain `http://` FileFin on a LAN, and ATS refuses
		     cleartext by default. NSAllowsLocalNetworking relaxes it for LOCAL
		     addressing only: link-local, .local, and RFC1918 style hosts.

		     NSAllowsArbitraryLoads is deliberately NOT set. It would relax ATS
		     for every host on the internet, which is a far larger grant than
		     "my NAS on the sofa" needs.

		     The consequence, stated rather than discovered later: a plain-http
		     FileFin on a routable PUBLIC address is still refused on iOS. F1's
		     http warning is worded for that reality, and
		     docs/verification-backlog.md carries the device experiment. -->
		<key>NSAllowsLocalNetworking</key>
		<true/>
	</dict>
	<!-- iOS 14+ prompts before the FIRST connection to a local-network address
	     and KILLS an app that has no usage string to show. This is not
	     cosmetic: without it the very first request to a LAN server terminates
	     the process. -->
	<key>NSLocalNetworkUsageDescription</key>
	<string>FileFin connects to your media server, which is usually on your local network.</string>
	<!-- F14, and the ONE key the iOS half needs. Measured both ways at
	     M7.6/E-6 on the simulator: without it the process is suspended the
	     moment it is backgrounded and the last position tick is the one that
	     follows `LIFECYCLE paused`; with it, playback runs 1:1 with the wall
	     clock for a full minute in the background.

	     Nothing sets an AVAudioSession category, and that is not an omission:
	     `nm -u` on media_kit_libs_ios_video's DEVICE slice shows libmpv itself
	     importing _AVAudioSessionCategoryPlayback, _AVAudioSessionModeMovie
	     Playback and setCategory:error:, which is mpv's own ao_audiounit. An
	     `audio_session` arm behaved identically to this one.

	     `fetch` and `processing` are deliberately absent: this app does nothing
	     in the background except keep decoding what the user started. -->
	<key>UIBackgroundModes</key>
	<array>
		<string>audio</string>
	</array>
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<false/>
		<key>UISceneConfigurations</key>
		<dict>
			<key>UIWindowSceneSessionRoleApplication</key>
			<array>
				<dict>
					<key>UISceneClassName</key>
					<string>UIWindowScene</string>
					<key>UISceneConfigurationName</key>
					<string>flutter</string>
					<key>UISceneDelegateClassName</key>
					<string>$(PRODUCT_MODULE_NAME).SceneDelegate</string>
					<key>UISceneStoryboardFile</key>
					<string>Main</string>
				</dict>
			</array>
		</dict>
	</dict>
	<key>UIApplicationSupportsIndirectInputEvents</key>
	<true/>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
</dict>
</plist>
