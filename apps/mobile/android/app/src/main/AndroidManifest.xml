<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Every screen in this app is a request to a server the user typed. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- F14. Not decoration: E-6 measured a backgrounded player still decoding
         and the OS muting it through AppOps CONTROL_AUDIO
         (`state:started mutedState:opControlAudio`). A foreground service in
         the mediaPlayback category is what lifts that, and from API 34 the
         typed permission below is required as well as the general one.
         WAKE_LOCK keeps the CPU awake while the screen is off; POST_
         NOTIFICATIONS is API 33+'s gate on the transport notification itself,
         and without it the service runs with nothing on screen to control it.
         docs/risks.md R3 carries the measurement and its negative control. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:label="FileFin"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">
        <!-- F14 + M7/E-11: extends AudioServiceActivity so the media session
             transport works, and adds a MethodChannel that exports the device
             CA trust store to a PEM file libmpv can verify against. -->
        <activity
            android:name="dev.filefin.filefin_mobile.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- F14's foreground service and the receiver that carries a hardware
             media key to it. `exported="true"` on both is required by the
             platform, not a choice: the MediaBrowserService is bound by
             SystemUI and Android Auto, and MEDIA_BUTTON is broadcast by the
             system. Neither takes an argument from the caller. -->
        <service
            android:name="com.ryanheise.audioservice.AudioService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService"/>
            </intent-filter>
        </service>
        <receiver
            android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON"/>
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
